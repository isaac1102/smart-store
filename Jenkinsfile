pipeline {
    agent any
 
    stages {
        stage('git checkout & clone') {
            steps {
				sh 'java -version'
            }
        }
        
        stage('print'){
        	steps{
        		sh 'echo hello world!'
        	}
        }
    }
}
